import {makeStyles} from "@material-ui/core";
import Colors from "../Colors";


export const navStyles = makeStyles({
  paper: {
    backgroundColor: Colors.primaryColorDark3,
    color: Colors.primaryColorLight1,
    fontFamily: 'Poppins',
  }
})
